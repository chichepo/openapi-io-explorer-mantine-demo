# OpenAPI I/O Explorer (Mantine) - Demo files

These are the minimal demo files (App Router) to show:
- Accordion: Microservice -> Endpoint -> Method
- Tree view: Request/Response schema

## Install
npm i @mantine/core @mantine/hooks @emotion/react

## Run
Use your existing Next.js app (App Router) and copy these files.
